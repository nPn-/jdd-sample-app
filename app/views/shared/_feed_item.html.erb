<li id="<%= feed_item.id %>">
  <%= link_to gravatar_for(feed_item.user), feed_item.user %>
  <span class="user">
    <%= link_to feed_item.user.name, feed_item.user %>
  </span>
  <%= render partial: 'shared/micropost_with_delete_link', locals: { micropost_item: feed_item } %>
</li>